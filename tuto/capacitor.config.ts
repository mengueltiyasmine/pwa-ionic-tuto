import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yasmin.tuto',
  appName: 'tuto',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
